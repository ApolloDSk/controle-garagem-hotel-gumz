# dependências de teste (raiz)
node_modules/
package-lock.json

# artefatos do Playwright
test-results/
playwright-report/
blob-report/
.playwright/

# backups locais
*.bak-*

# amostras reais de PDF (contêm dados de hóspedes — NÃO versionar); só o README fica no repo
amostras/*.pdf
